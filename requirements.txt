# ══════════════════════════════════════════════════════════════════════════════
# CentrixSupport — Python dependencies
# ══════════════════════════════════════════════════════════════════════════════
#
# Install:
#   pip install -r requirements.txt
#
# Python >= 3.10 required.
#
# System prerequisites (must be installed separately — not via pip):
#   • Tesseract OCR  → https://github.com/tesseract-ocr/tesseract
#       Windows: winget install UB-Mannheim.TesseractOCR
#       Ubuntu:  sudo apt install tesseract-ocr
#   • FFmpeg (required by openai-whisper for audio files)
#       Windows: winget install Gyan.FFmpeg
#       Ubuntu:  sudo apt install ffmpeg
#   • MongoDB Community (optional — only needed for conversation/mood/task
#       persistence; app degrades gracefully without it)
#       https://www.mongodb.com/try/download/community
#
# ── Environment variables ──────────────────────────────────────────────────────
# Copy .env.example → .env and fill in your keys before running.
# ══════════════════════════════════════════════════════════════════════════════

# ── Web framework ─────────────────────────────────────────────────────────────
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.1.3
gunicorn==21.2.0

# ── Environment / config ──────────────────────────────────────────────────────
python-dotenv==1.2.1

# ── AI / LLM providers ────────────────────────────────────────────────────────
# Primary LLM: Groq (openai/gpt-oss-120b)
groq==1.5.0
# Fallback LLM: Google Gemini (gemini-3.5-flash-lite)
google-generativeai==0.8.5

# ── LlamaIndex RAG stack ───────────────────────────────────────────────────────
llama-index-core==0.14.23
llama-index-embeddings-huggingface==0.7.0
llama-index-llms-groq==0.5.0
llama-index-readers-file==0.5.6

# ── LangChain (pulled in transitively by llama-index; pinned for stability) ───
langchain==0.3.27
langchain-core==0.3.74
langchain-community==0.3.27

# ── Embeddings & cross-encoder reranking ──────────────────────────────────────
sentence-transformers==3.0.1

# ── PyTorch (CPU) — required by sentence-transformers / Whisper ───────────────
# NOTE: If you have an NVIDIA GPU, replace these three lines with the
#       GPU variant from https://pytorch.org/get-started/locally/
#       e.g.  pip install torch==2.3.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# ── Transformers / tokenizers (HuggingFace) ───────────────────────────────────
transformers==4.44.2
tokenizers==0.19.1
huggingface-hub==0.36.2
tiktoken==0.9.0

# ── Vector search ─────────────────────────────────────────────────────────────
faiss-cpu==1.10.0

# ── Numerical / scientific ────────────────────────────────────────────────────
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.2

# ── Document parsing ──────────────────────────────────────────────────────────
pdfplumber==0.11.5
python-docx==1.1.2
pytesseract==0.3.13
# Headless OpenCV — no display required (use opencv-python if you have a GUI)
opencv-python-headless==4.13.0.92
Pillow==12.3.0

# ── Audio transcription (Whisper) ─────────────────────────────────────────────
# PyPI package name is openai-whisper (not "whisper")
openai-whisper==20240930
ffmpeg-python==0.2.0
soundfile==0.13.1
numba==0.61.0
llvmlite==0.44.0

# ── Audio playback (mental_exercises.py) ──────────────────────────────────────
playsound==1.2.2

# ── NLP ───────────────────────────────────────────────────────────────────────
nltk==3.10.0
regex==2026.7.19

# ── MongoDB — conversation, mood, and task persistence ────────────────────────
# bson is bundled inside pymongo — do NOT install the standalone bson package
pymongo==4.11.2

# ── Observability — Langfuse v4 ───────────────────────────────────────────────
langfuse==4.6.1
# OpenTelemetry (required by Langfuse v4 OTel-based tracing)
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0

# ── HTTP client ───────────────────────────────────────────────────────────────
requests==2.34.2
# SerpApi — primary web search provider (DuckDuckGo is the fallback)
serpapi==0.1.5
httpx==0.28.1
httpcore==1.0.9
anyio==4.14.2
mcp==1.30.0
jsonschema==4.26.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm==4.69.0
more-itertools==10.7.0
polars==1.35.2
